<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="temperatureTableText">
        <item name="android:textColor">@color/table_text</item>
        <item name="android:textSize">16sp</item>
        <item name="android:layout_height">42dp</item>
        <item name="android:width">77dp</item>
        <item name="android:gravity">center</item>
    </style>

    <style name="tableText">
        <item name="android:textColor">@color/table_text</item>
        <item name="android:textSize">16sp</item>
        <item name="android:layout_height">42dp</item>
        <item name="android:width">77dp</item>
        <item name="android:gravity">center</item>
    </style>


    <!--体温测量弹窗-->
    <style name="temperaturePromptDialog" parent="@android:style/Theme.Dialog">
        <!-- 边框 -->
        <item name="android:windowFrame">@null</item>
        <!-- 是否浮现在activity之上 -->
        <item name="android:windowIsFloating">true</item>
        <!-- 半透明 -->
        <item name="android:windowIsTranslucent">false</item>
        <!-- 无标题 -->
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <!-- 背景透明 -->
        <item name="android:backgroundDimEnabled">true</item>
        <!-- 不可取消-->
        <item name="android:windowCloseOnTouchOutside">true</item>
        <!-- 模糊 -->
        <item name="android:backgroundDimAmount">0.8</item>
    </style>

</resources>
